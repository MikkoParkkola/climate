Build a secure, production-ready web application that lets users select any location on Earth (via an interactive map interface, e.g., Mapbox or Leaflet), choose a future time horizon (e.g., 2030, 2050, 2100), and view a graphical projection of climate development (temperature, precipitation, etc.) for that spot using the Nvidia Earth-2/Climate in a Bottle API.

Requirements: • Use TypeScript (React + Next.js) or Python (FastAPI + React frontend). • The UI must include: ▫ An interactive world map for browsing and selecting locations. ▫ A date picker or slider for selecting the future year. ▫ A “Get Projection” button that fetches and displays climate data for the selected spot and time. ▫ Graphical output: charts (matplotlib or Chart.js) and map overlays for projected variables. • Integrate securely with the Nvidia API (assume API key is stored server-side, never exposed to the client). • Handle API errors and rate limits gracefully. • Include basic unit tests for API integration and UI logic. • Provide a threat model: list main security risks (API key leakage, XSS, SSRF, etc.) and how you mitigate them. • Use secure-by-default coding patterns (OWASP Top-10). • Provide clear docstrings/JSDoc for all functions/components.

Bonus: • Allow users to compare projections for two different locations or time periods side-by-side. • Add a download button for the projection data as CSV.

Example prompt for ChatGPT/Copilot:

Write a TypeScript (React + Next.js) web app with a secure FastAPI backend that lets users select a location on a world map, pick a future year, and view climate projections (temperature, precipitation, etc.) for that spot using the Nvidia Earth-2/Climate in a Bottle API. The UI should have a map, a year picker, and display results as charts and map overlays. The backend should securely call the Nvidia API (API key never exposed to frontend), handle errors, and provide endpoints for the frontend. Include unit tests, docstrings/JSDoc, and a threat model covering API key security, XSS, SSRF, and rate limiting. Use secure-by-default patterns and OWASP Top-10 best practices.