import assert from "node:assert/strict";
import fs from "node:fs";
import path from "node:path";
import { fileURLToPath } from "node:url";

const repoRoot = path.resolve(path.dirname(fileURLToPath(import.meta.url)), "..");
const appPath = path.join(repoRoot, "client", "src", "pages", "climate-app.tsx");
const methodologyPath = path.join(repoRoot, "client", "src", "pages", "methodology.tsx");
const scoreSensitivityPath = path.join(repoRoot, "client", "src", "components", "score-sensitivity.tsx");
const source = fs.readFileSync(appPath, "utf8");
const helpers = fs.readFileSync(path.join(repoRoot, "client", "src", "lib", "climate-helpers.ts"), "utf8");
const charts = fs.readFileSync(path.join(repoRoot, "client", "src", "components", "climate-charts.tsx"), "utf8");
const shareCard = fs.readFileSync(path.join(repoRoot, "client", "src", "lib", "share-card.ts"), "utf8");
const constants = fs.readFileSync(path.join(repoRoot, "client", "src", "lib", "climate-constants.ts"), "utf8");
const types = fs.readFileSync(path.join(repoRoot, "client", "src", "lib", "climate-types.ts"), "utf8");
const derivations = fs.readFileSync(path.join(repoRoot, "client", "src", "lib", "climate-derivations.ts"), "utf8");
const landing = fs.readFileSync(path.join(repoRoot, "client", "src", "components", "climate-landing.tsx"), "utf8");
const hook = fs.readFileSync(path.join(repoRoot, "client", "src", "hooks", "use-climate-app.ts"), "utf8");
const resultView = fs.readFileSync(path.join(repoRoot, "client", "src", "components", "climate-result-view.tsx"), "utf8");
const sectionsTop = fs.readFileSync(path.join(repoRoot, "client", "src", "components", "climate-result-sections-top.tsx"), "utf8");
const sectionsBottom = fs.readFileSync(path.join(repoRoot, "client", "src", "components", "climate-result-sections-bottom.tsx"), "utf8");
const conditionsDrawer = fs.readFileSync(path.join(repoRoot, "client", "src", "components", "your-conditions-drawer.tsx"), "utf8");
const all = [source, helpers, charts, shareCard, constants, types, derivations, landing, hook, resultView, sectionsTop, sectionsBottom, conditionsDrawer].join("\n");
const methodology = fs.readFileSync(methodologyPath, "utf8");
const scoreSensitivity = fs.readFileSync(scoreSensitivityPath, "utf8");

assert.match(all, /const scoreStory = useMemo/, "storyline derives score-story data from the selected trajectory");
assert.match(all, /baseline\.habitability\.breakdown/, "storyline compares selected score components to the baseline breakdown");
assert.match(all, /componentScoreEffect/, "storyline adjusts contribution and penalty signs before ranking score drivers");
assert.match(all, /scoreDrivers[\s\S]*?sort\(\(a, b\) => Math\.abs\(b\.effect\) - Math\.abs\(a\.effect\)\)/, "storyline ranks drivers by absolute score effect");
assert.match(all, /Math\.abs\(driver\.effect\) >= 0\.05/, "storyline filters out near-zero driver movement before ranking");
assert.match(all, /perDecade/, "storyline exposes per-decade trend rates, not only endpoint deltas");
assert.match(all, /lowValues\?: number\[\]/, "trend charts accept low/high uncertainty series");
assert.match(all, /const annualYears = Array\.from\(\{ length: MAX_YEAR - BASELINE_YEAR \+ 1 \}/, "trend charts render annual displayed points through 2100");
assert.match(all, /interpArr\(years, values, yr\)/, "trend charts interpolate annual displayed values from grounded trajectory checkpoints");
assert.match(all, /function ChartValuesDetails/, "trend charts expose a keyboard and touch accessible values disclosure");
assert.match(all, /<ChartValuesDetails label=\{label\} rows=\{valueRows\} \/>/, "each trend chart wires the values disclosure");
assert.match(all, /<title>\{pointLabel\(i\)\}<\/title>/, "each plotted year exposes a hover value title");
assert.match(all, /linear interpolation between grounded API checkpoints/, "chart point labels disclose interpolation instead of pretending annual model granularity");
assert.match(all, /Year basis/, "projection receipt exposes the source-year basis");
assert.match(all, /packed scenario layers start at 2030/, "current-year sampling copy discloses source-layer cadence");
assert.match(all, /projectionYearBasis/, "result snapshot carries projection-year basis metadata from the API");
assert.match(all, /scenarioLabel\?: string/, "trend charts can display the active scenario label");
assert.match(all, /scenarioLabel=\{shownScenario\.label\}/, "metric trajectory charts pass the active scenario label");
assert.match(all, /const rangeD = hasRange/, "trend charts render a visual uncertainty band when low/high series are present");
assert.match(all, /thresholdLabel\?: string/, "trend charts can label documented threshold markers directly on the graph");
assert.match(all, /Threshold marker: \$\{thresholdLabel\}/, "trend chart accessible label includes the threshold marker label");
assert.match(all, /temperature\.uncertainty\?\.annual_mean_low/, "temperature chart uses grounded temperature low/high uncertainty fields");
assert.match(all, /precipitation\.uncertainty\?\.annual_total_low/, "precipitation chart uses grounded precipitation low/high uncertainty fields");
assert.match(all, /sea_level_low_cm/, "sea-level context chart uses grounded AR6 low/high context fields");
assert.match(all, /flood: trajectory\.map\(\(p\) => riskScore\(p\.extremes\.flood_risk\)\)/, "metric trajectories include the grounded flood-risk series");
assert.match(all, /values=\{traj!\.flood\}[\s\S]*?label="Flood Risk"/, "result page renders a flood-risk trend chart");
assert.match(all, /thresholdLabel="50% elevated risk"/, "risk trend charts mark the documented elevated-risk threshold directly");
assert.match(all, /thresholdLabel=\{coastalRelevance\?\.thresholdLabel \?\? "50 cm regional context"\}/, "sea-level context chart labels its coastal or regional context marker");
assert.match(all, /Translucent bands show grounded low-high ranges/, "chart legend explains range bands only appear where comparable uncertainty fields exist");
assert.match(all, /all seven charts/, "chart legend accounts for temperature, precipitation, heat, habitability, sea-level, drought, and flood");
assert.match(all, /Hover plotted years for values, or open values for keyboard\/touch access/, "chart legend explains hover and non-hover value access");
assert.match(all, /<ReceiptDetails label="range" text=\{uncertaintyLabel\} \/>/, "range bands expose keyboard and touch accessible method receipts");
assert.match(all, /<ReceiptDetails label="threshold" text=\{`Dashed marker:/, "threshold markers expose keyboard and touch accessible receipts");
assert.match(all, /const roadmapItems = useMemo/, "result page derives a living-conditions roadmap from the selected trajectory");
assert.match(all, /ROADMAP_YEARS/, "roadmap includes the current year and decade waypoints through 2100");
assert.match(all, /Roadmap · current year to 2100/, "result page renders the required roadmap section");
assert.match(all, /5-year checkpoints through 2100 and linearly interpolates intermediate years/, "roadmap method receipt discloses checkpoint cadence and interpolation");
assert.match(all, /Sea-level context/, "roadmap includes sea-level context as a visible metric");
assert.match(all, /coastal-proximity\.natural-earth-110m\.json/, "sea-level copy loads the registered Natural Earth coastal-proximity artifact");
assert.match(all, /Coarse nearest-coast distance is/, "sea-level receipt must disclose the coarse nearest-coast method");
assert.match(all, /Natural Earth 1:110m coastline/, "sea-level receipt must disclose the coastline source");
assert.match(all, /no elevation, tides, storm surge, subsidence, coastal defenses, rivers, drainage, or parcel exposure/, "sea-level receipt must reject parcel exposure and missing local factors");
assert.match(all, /Coastal relevance:/, "educational export includes coastal relevance status");
assert.match(all, /SSP3-7\.0 vs SSP1-2\.6/, "roadmap can show lower-vs-higher pathway deltas after scenario contrast loads");
assert.match(all, /Load pathway contrast to add lower-vs-higher scenario deltas/, "roadmap discloses missing scenario deltas before contrast is loaded");
assert.match(all, /Why this changed/, "result page renders the required why-this-changed section");
assert.match(all, /const scoreSensitivityInputs = useMemo/, "result page derives score-sensitivity inputs from the selected breakdown");
assert.match(all, /makeInput\("temperature_comfort"/, "score sensitivity includes temperature comfort from habitability.breakdown");
assert.match(all, /makeInput\("precipitation_adequacy"/, "score sensitivity includes precipitation adequacy from habitability.breakdown");
assert.match(all, /makeInput\("humid_heat_penalty"/, "score sensitivity includes the wet-bulb humid-heat penalty from habitability.breakdown");
assert.match(all, /makeInput\("heat_stress_penalty"/, "score sensitivity includes heat-stress penalty from habitability.breakdown");
assert.match(all, /makeInput\("drought_penalty"/, "score sensitivity includes drought penalty from habitability.breakdown");
assert.match(all, /makeInput\("flood_penalty"/, "score sensitivity includes flood penalty from habitability.breakdown");
assert.match(all, /breakdown=\{scoreSensitivityInputs\}/, "result page wires the selected score-sensitivity inputs into the conditions drawer");
assert.match(all, /<ScoreBreakdown inputs=\{breakdown\} modelScore=\{modelScore\} category=\{category\} \/>/, "conditions drawer renders the score breakdown with selected model score, category, and inputs");
assert.match(scoreSensitivity, /How this score is built/, "score breakdown panel has an explicit heading");
assert.match(scoreSensitivity, /These are the grounded pieces of your score/, "score breakdown discloses its components are grounded, not a separate what-if forecast");
assert.match(scoreSensitivity, /kind: "contribution" \| "penalty"/, "score breakdown distinguishes grounded contributions from penalties");
assert.match(scoreSensitivity, /\{modelScore\}\/100/, "score breakdown shows the grounded model score out of 100");
assert.match(methodology, /Habitability score/, "methodology documents how the habitability score is built (formula transparency)");
assert.match(all, /What this means for daily life/, "result page renders the required daily-life interpretation section");
assert.match(all, /function circulationContextFor/, "storyline can surface registered AMOC/Gulf Stream context for relevant broad regions");
assert.match(all, /AMOC\/Gulf Stream context/, "daily-life section includes AMOC/Gulf Stream context when regionally relevant");
assert.match(all, /very likely to weaken during this century/, "AMOC context states the expected weakening without hiding the science");
assert.match(all, /not the central IPCC assessment/, "AMOC context caveats abrupt collapse before 2100");
assert.match(all, /no deterministic local correction or collapse date is applied/, "AMOC context does not overclaim local correction or timing");
assert.match(methodology, /Large-scale circulation context/, "methodology documents AMOC/Gulf Stream context");
assert.match(methodology, /does not apply a local cooling correction/, "methodology rejects deterministic AMOC local correction");
assert.match(methodology, /scenario layers start at 2030/, "methodology discloses near-current source-year basis");
assert.match(methodology, /Humid heat screen/, "methodology documents the humid-heat screen");
assert.match(methodology, /Stull 2011 empirical wet-bulb approximation/, "methodology cites the humid-heat formula");
assert.match(methodology, /not WBGT or occupational-safety guidance/, "methodology caveats humid heat as non-WBGT and non-advisory");
assert.match(all, /What this does not mean/, "projection receipt renders the required caveat section");
assert.match(all, /not a property-risk certificate/, "receipt caveat rejects property-risk interpretation");
assert.match(all, /relocation recommendation/, "receipt caveat rejects relocation advice interpretation");
assert.match(all, /parcel-scale exposure/, "receipt caveat discloses missing parcel-scale exposure");
assert.match(all, /not a full causal attribution model/, "driver ranking caveats the attribution boundary");
assert.match(all, /function ReceiptDetails\(/, "result page centralizes receipt disclosures in an accessible component");
assert.match(all, /aria-label=\{`\$\{label\}: \$\{text\}`\}/, "receipt disclosure summaries expose their full text to assistive tech");
assert.match(all, /role="note"/, "expanded receipt disclosures are announced as notes");
assert.match(all, /<ReceiptDetails label="source" text=\{signal\.receipt\} \/>/, "daily-life rows expose keyboard and touch accessible source receipts");
assert.doesNotMatch(source, /title=\{signal\.receipt\}/, "daily-life source receipts must not regress to hover-only title text");
assert.match(all, /Not yet included in the score/, "storyline discloses important missing impact domains");
assert.match(all, /Raw CMIP6 model-consensus annual_mean and anomaly/, "temperature KPI exposes a source receipt");
assert.match(all, /groundwater, reservoirs, demand, or local drainage capacity/, "precipitation KPI discloses missing local water-system factors");
assert.match(all, /climate screening indicator, not medical or occupational-safety advice/, "heat-stress KPI has a non-advisory receipt");
assert.match(all, /not a safety certificate or relocation recommendation/, "habitability KPI caveats interpretation");
assert.match(all, /tropical_nights_per_year\?: number/, "risk cards type the raw ETCCDI tropical-nights value");
assert.match(all, /consecutive_dry_days\?: number/, "risk cards type the raw ETCCDI consecutive-dry-days value");
assert.match(all, /max_5day_precip_mm\?: number/, "risk cards type the raw ETCCDI Rx5day value");
assert.match(all, /function interpOptionalScalar/, "optional raw ETCCDI values are not fabricated when absent");
assert.match(all, /if \(av == null \|\| bv == null\) return undefined/, "optional raw ETCCDI interpolation fails closed when bracket data is missing");
assert.match(all, /const drySpellDays = interpOptionalScalar\(pts, year, \(p\) => p\.extremes\.detail\?\.consecutive_dry_days\)/, "missing drought raw detail is not defaulted to zero");
assert.match(all, /const maxFiveDayRain = interpOptionalScalar\(pts, year, \(p\) => p\.extremes\.detail\?\.max_5day_precip_mm\)/, "missing flood raw detail is not defaulted to zero");
assert.match(all, /Heat stress uses the grounded ETCCDI tropical-nights layer/, "heat-stress risk card exposes its source receipt");
assert.match(all, /Humid heat screen/, "risk cards expose the humid-heat screening metric");
assert.match(all, /Stull 2011 empirical wet-bulb approximation/, "humid-heat receipt cites the registered wet-bulb method");
assert.match(all, /not WBGT, not daily humid-heat days/, "humid-heat receipt rejects WBGT and daily exceedance interpretations");
assert.match(all, /temperature-domain warning months/, "humid-heat receipt discloses Stull formula temperature-domain warnings");
assert.match(all, /Humid heat feel/, "daily-life section exposes humid-heat interpretation");
assert.match(all, /d\.humidHeatWetBulb/, "daily-life humid-heat row is derived from the API snapshot");
assert.match(all, /monthly mean wet-bulb/, "humid-heat copy labels the metric as monthly mean context");
assert.match(all, /function countMonthlyFreezeContext/, "cold-season context derives from monthly temperature values");
assert.match(all, /const coldMonthCount = countMonthlyFreezeContext\(monthlyTemps\)/, "selected-year snapshot counts monthly-mean freeze months");
assert.match(all, /Cold-season context/, "daily-life and risk sections expose cold-season context");
assert.match(all, /monthly mean freeze months/, "cold-season UI labels the metric as monthly-mean freeze context");
assert.match(all, /not daily freeze days/, "cold-season receipts reject daily freeze-day interpretation");
assert.match(all, /not a daily cold-stress count/, "cold-season copy rejects daily cold-stress interpretation");
assert.match(all, /Cold months/, "roadmap surfaces cold-season context alongside heat, water, sea level, and score");
assert.match(methodology, /Cold-season context/, "methodology documents cold-season context");
assert.match(methodology, /cannot\s+count daily freeze days/, "methodology discloses cold-season proxy limits");
assert.match(all, /Drought risk uses ETCCDI consecutive dry days/, "drought risk card exposes its source receipt");
assert.match(all, /Flood risk uses ETCCDI Rx5day/, "flood risk card exposes its source receipt");
assert.match(all, /Sea-level context uses the registered NASA\/IPCC AR6 regional sea-level layer/, "sea-level context card exposes its source receipt");
assert.match(all, /dry-spell days.*raw/, "drought risk card shows the raw dry-spell quantity alongside the score");
assert.match(all, /mm Rx5day.*raw/, "flood risk card shows the raw Rx5day quantity alongside the score");
assert.match(all, /It is not a parcel flood map or insurance loss estimate/, "flood receipt rejects parcel and insurance interpretations");
assert.match(all, /\/\* Risk & Extremes \*\/[\s\S]*?repeat\(auto-fit,minmax\(170px,1fr\)\)/, "risk cards remain responsive after adding receipts");
assert.match(all, /Use the scenario contrast as a learning tool/, "scenario call to action stays educational rather than preachy");
assert.doesNotMatch(source, /Everything you just saw is a dare/, "scenario call to action must not use preachy dare framing");

console.log("storyline smoke passed: roadmap, explainability sections, annual trend point values, risk receipts, AMOC context, and accessible receipts guarded");
